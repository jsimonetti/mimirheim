{
  "$defs": {
    "HomeAssistantConfig": {
      "additionalProperties": false,
      "description": "Home Assistant MQTT discovery settings for a helper tool.\n\nWhen ``enabled`` is True, the daemon publishes a retained discovery payload\nto ``{discovery_prefix}/button/{tool_name}/config`` on every broker connect\nand whenever HA's birth message (``homeassistant/status = online``) is\nreceived. This creates a button entity in HA that triggers the tool on press.\n\nAttributes:\n    enabled: Enable or disable HA discovery for this tool. Defaults to False\n        for backward compatibility \u2014 existing configs that do not have a\n        ``homeassistant:`` section are unaffected.\n    discovery_prefix: HA discovery topic prefix. Defaults to ``homeassistant``.\n    device_name: Human-readable display name for the HA device card. If\n        omitted, the daemon uses a formatted version of the tool name.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable HA MQTT discovery.",
          "title": "Enable HA discovery",
          "type": "boolean",
          "x-category": "Advanced"
        },
        "discovery_prefix": {
          "default": "homeassistant",
          "description": "HA MQTT discovery topic prefix.",
          "title": "Discovery prefix",
          "type": "string",
          "x-category": "Advanced"
        },
        "device_name": {
          "default": "",
          "description": "Display name for the HA device. Defaults to tool name.",
          "title": "HA device name",
          "type": "string",
          "x-category": "Advanced"
        },
        "forecast_sensor": {
          "default": true,
          "description": "Publish an additional HA sensor entity for the helper's forecast output topic.",
          "title": "Enable forecast sensor",
          "type": "boolean",
          "x-category": "Advanced"
        }
      },
      "title": "HomeAssistantConfig",
      "type": "object",
      "x-categoryOrder": [
        "Basic",
        "Advanced"
      ],
      "x-format": "categories-vertical"
    },
    "MqttConfig": {
      "additionalProperties": false,
      "description": "MQTT broker connection parameters, shared by all mimirheim input helper daemons.\n\nAttributes:\n    host: Broker hostname or IP address.\n    port: Broker TCP port. Default is 1883 (unencrypted); use 8883 with TLS.\n    client_id: MQTT client identifier. Must be unique on the broker.\n    username: Optional broker username. Omit for anonymous access.\n    password: Optional broker password.\n    tls: Enable TLS for the broker connection. Set to True when the broker\n        listens on an encrypted port (typically 8883). When False, a\n        plaintext connection is made regardless of the port number.\n    tls_allow_insecure: When True and tls is also True, skip broker\n        certificate verification. Useful for self-signed certificates on\n        private networks. Has no effect when tls is False. Do not use\n        against a broker reachable from an untrusted network.",
      "properties": {
        "host": {
          "description": "Broker hostname or IP address.",
          "title": "Broker host",
          "type": "string"
        },
        "port": {
          "default": 1883,
          "description": "Broker TCP port.",
          "maximum": 65535,
          "minimum": 1,
          "title": "Broker port",
          "type": "integer",
          "x-category": "Advanced"
        },
        "client_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "MQTT client identifier. Defaults to a tool-specific value when not set.",
          "title": "Client ID"
        },
        "username": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Broker username.",
          "title": "Username",
          "x-category": "Advanced"
        },
        "password": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Broker password.",
          "title": "Password",
          "x-category": "Advanced"
        },
        "tls": {
          "default": false,
          "description": "Enable TLS for the broker connection. Set to true when the broker listens on an encrypted port (typically 8883).",
          "title": "Enable TLS",
          "type": "boolean",
          "x-category": "Advanced"
        },
        "tls_allow_insecure": {
          "default": false,
          "description": "Skip broker certificate verification when TLS is enabled. Has no effect when tls is false.",
          "title": "Allow insecure TLS",
          "type": "boolean",
          "x-category": "Advanced"
        }
      },
      "required": [
        "host"
      ],
      "title": "MqttConfig",
      "type": "object",
      "x-categoryOrder": [
        "Basic",
        "Advanced"
      ],
      "x-format": "categories-vertical"
    },
    "StaticBaseloadConfig": {
      "additionalProperties": false,
      "description": "Parameters that define the static load profile.\n\nArgs:\n    profile_kw: Power values in kilowatts that form the repeating cycle\n        used when no per-weekday override applies. The most common use is\n        24 values, one per hour-of-day (00:00\u201323:00 UTC). Any length from\n        1 to 168 is accepted; the profile is tiled via wall-clock UTC hour\n        modulo. Required unless ``weekly_profiles_kw`` covers all 7\n        weekdays (0=Monday \u2026 6=Sunday).\n    weekly_profiles_kw: Optional per-weekday profiles in kilowatts, keyed\n        by Python weekday number (0=Monday, 1=Tuesday, \u2026 6=Sunday). Each\n        entry must follow the same rules as ``profile_kw``: 1\u2013168 values\n        indexed by UTC hour-of-day via modulo. When a step's weekday has\n        an entry here it takes precedence over ``profile_kw``. If all 7\n        days are provided, ``profile_kw`` may be omitted entirely.\n    horizon_hours: Number of hourly steps to publish, starting from the\n        current wall-clock hour. Defaults to 48. Must be between 1 and 168.",
      "properties": {
        "profile_kw": {
          "anyOf": [
            {
              "items": {
                "type": "number"
              },
              "maxItems": 168,
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Hourly profile (kW)"
        },
        "weekly_profiles_kw": {
          "anyOf": [
            {
              "additionalProperties": {
                "items": {
                  "type": "number"
                },
                "type": "array"
              },
              "type": "object",
              "default": {}
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Weekly profiles (kW)",
          "x-category": "Advanced"
        },
        "horizon_hours": {
          "default": 48,
          "maximum": 168,
          "minimum": 1,
          "title": "Horizon (hours)",
          "type": "integer",
          "x-category": "Advanced"
        }
      },
      "title": "StaticBaseloadConfig",
      "type": "object",
      "x-categoryOrder": [
        "Basic",
        "Advanced"
      ],
      "x-format": "categories-vertical"
    }
  },
  "additionalProperties": false,
  "description": "Root configuration for the static baseload daemon.\n\nArgs:\n    mqtt: MQTT broker connection settings.\n    mimir_topic_prefix: The ``mqtt.topic_prefix`` configured in mimirheim. Used\n        to derive default values for ``output_topic`` and\n        ``mimir_trigger_topic``. Defaults to ``\"mimir\"``.\n    mimir_static_load_name: The ``static_loads`` device name in the mimirheim config\n        that this tool publishes to. Used to derive the default\n        ``output_topic`` when it is not set explicitly. Defaults to\n        ``\"base_load\"``.\n    trigger_topic: The tool subscribes here; a message fires one publish cycle.\n    output_topic: Base load forecast payload is published retained to this topic.\n        Defaults to the mimirheim canonical baseload topic derived from\n        ``mimir_topic_prefix`` and ``mimir_static_load_name``.\n    baseload: Static load profile and horizon configuration.\n    signal_mimir: If True, publish to mimir_trigger_topic after publishing the\n        forecast.\n    mimir_trigger_topic: Required when signal_mimir is True. Defaults to the\n        mimirheim canonical trigger topic derived from ``mimir_topic_prefix``.",
  "properties": {
    "mqtt": {
      "$ref": "#/$defs/MqttConfig",
      "description": "MQTT broker connection settings.",
      "title": "MQTT"
    },
    "mimir_topic_prefix": {
      "default": "mimir",
      "description": "mimirheim mqtt.topic_prefix. Used to derive default output and trigger topics.",
      "title": "mimirheim topic prefix",
      "type": "string",
      "x-category": "Advanced"
    },
    "mimir_static_load_name": {
      "default": "base_load",
      "description": "mimirheim static_loads device name. Used to derive the default output_topic.",
      "title": "mimirheim static load name",
      "type": "string",
      "x-category": "Advanced",
      "x-enumSource": "#/context/static_loads"
    },
    "trigger_topic": {
      "description": "MQTT topic that triggers a publish cycle.",
      "title": "Trigger topic",
      "type": "string",
      "x-category": "Advanced"
    },
    "output_topic": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "MQTT topic for the retained baseload forecast payload. Defaults to '{mimir_topic_prefix}/input/baseload/{mimir_static_load_name}/forecast'.",
      "title": "Output topic",
      "x-category": "Advanced",
      "x-template": "{{ topic_prefix.value || 'mimir' }}/input/baseload/{{ load_name.value || 'base_load' }}/forecast",
      "x-watch": {
        "load_name": "#/mimir_static_load_name",
        "topic_prefix": "#/mimir_topic_prefix"
      }
    },
    "baseload": {
      "$ref": "#/$defs/StaticBaseloadConfig",
      "description": "Static load profile and horizon configuration.",
      "title": "Baseload profile"
    },
    "signal_mimir": {
      "default": false,
      "description": "Publish to mimir_trigger_topic after publishing the forecast.",
      "title": "Signal mimirheim",
      "type": "boolean",
      "x-category": "Advanced"
    },
    "mimir_trigger_topic": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "mimirheim trigger topic. Derives from mimir_topic_prefix when not set.",
      "title": "mimirheim trigger topic",
      "x-category": "Advanced"
    },
    "ha_discovery": {
      "anyOf": [
        {
          "$ref": "#/$defs/HomeAssistantConfig",
          "default": {}
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional Home Assistant MQTT discovery settings.",
      "title": "HA discovery",
      "x-category": "Advanced"
    },
    "stats_topic": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "MQTT topic where per-cycle run statistics are published.",
      "title": "Stats topic",
      "x-category": "Advanced"
    }
  },
  "required": [
    "mqtt",
    "trigger_topic",
    "baseload"
  ],
  "title": "BaseloadConfig",
  "type": "object",
  "x-categoryOrder": [
    "Basic",
    "Advanced"
  ],
  "x-format": "categories-vertical",
  "x-mimirheim": {
    "file": "baseload-static.yaml",
    "category": "baseload",
    "order": 0,
    "python_package": "baseload_static",
    "python_model": "baseload_static.config:BaseloadConfig",
    "exclusive_group": "baseload",
    "required": false,
    "docs_url": null
  }
}

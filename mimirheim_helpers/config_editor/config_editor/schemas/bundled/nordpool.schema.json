{
  "$defs": {
    "HomeAssistantConfig": {
      "additionalProperties": false,
      "description": "Home Assistant MQTT discovery settings for a helper tool.\n\nWhen ``enabled`` is True, the daemon publishes a retained discovery payload\nto ``{discovery_prefix}/button/{tool_name}/config`` on every broker connect\nand whenever HA's birth message (``homeassistant/status = online``) is\nreceived. This creates a button entity in HA that triggers the tool on press.\n\nAttributes:\n    enabled: Enable or disable HA discovery for this tool. Defaults to False\n        for backward compatibility \u2014 existing configs that do not have a\n        ``homeassistant:`` section are unaffected.\n    discovery_prefix: HA discovery topic prefix. Defaults to ``homeassistant``.\n    device_name: Human-readable display name for the HA device card. If\n        omitted, the daemon uses a formatted version of the tool name.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable HA MQTT discovery.",
          "title": "Enable HA discovery",
          "type": "boolean",
          "x-category": "Advanced"
        },
        "discovery_prefix": {
          "default": "homeassistant",
          "description": "HA MQTT discovery topic prefix.",
          "title": "Discovery prefix",
          "type": "string",
          "x-category": "Advanced"
        },
        "device_name": {
          "default": "",
          "description": "Display name for the HA device. Defaults to tool name.",
          "title": "HA device name",
          "type": "string",
          "x-category": "Advanced"
        },
        "forecast_sensor": {
          "default": true,
          "description": "Publish an additional HA sensor entity for the helper's forecast output topic.",
          "title": "Enable forecast sensor",
          "type": "boolean",
          "x-category": "Advanced"
        }
      },
      "title": "HomeAssistantConfig",
      "type": "object",
      "x-categoryOrder": [
        "Basic",
        "Advanced"
      ],
      "x-format": "categories-vertical"
    },
    "MqttConfig": {
      "additionalProperties": false,
      "description": "MQTT broker connection parameters, shared by all mimirheim input helper daemons.\n\nAttributes:\n    host: Broker hostname or IP address.\n    port: Broker TCP port. Default is 1883 (unencrypted); use 8883 with TLS.\n    client_id: MQTT client identifier. Must be unique on the broker.\n    username: Optional broker username. Omit for anonymous access.\n    password: Optional broker password.\n    tls: Enable TLS for the broker connection. Set to True when the broker\n        listens on an encrypted port (typically 8883). When False, a\n        plaintext connection is made regardless of the port number.\n    tls_allow_insecure: When True and tls is also True, skip broker\n        certificate verification. Useful for self-signed certificates on\n        private networks. Has no effect when tls is False. Do not use\n        against a broker reachable from an untrusted network.",
      "properties": {
        "host": {
          "description": "Broker hostname or IP address.",
          "title": "Broker host",
          "type": "string"
        },
        "port": {
          "default": 1883,
          "description": "Broker TCP port.",
          "maximum": 65535,
          "minimum": 1,
          "title": "Broker port",
          "type": "integer",
          "x-category": "Advanced"
        },
        "client_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "MQTT client identifier. Defaults to a tool-specific value when not set.",
          "title": "Client ID"
        },
        "username": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Broker username.",
          "title": "Username",
          "x-category": "Advanced"
        },
        "password": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Broker password.",
          "title": "Password",
          "x-category": "Advanced"
        },
        "tls": {
          "default": false,
          "description": "Enable TLS for the broker connection. Set to true when the broker listens on an encrypted port (typically 8883).",
          "title": "Enable TLS",
          "type": "boolean",
          "x-category": "Advanced"
        },
        "tls_allow_insecure": {
          "default": false,
          "description": "Skip broker certificate verification when TLS is enabled. Has no effect when tls is false.",
          "title": "Allow insecure TLS",
          "type": "boolean",
          "x-category": "Advanced"
        }
      },
      "required": [
        "host"
      ],
      "title": "MqttConfig",
      "type": "object",
      "x-categoryOrder": [
        "Basic",
        "Advanced"
      ],
      "x-format": "categories-vertical"
    },
    "NordpoolApiConfig": {
      "additionalProperties": false,
      "description": "Nordpool-specific fetch and pricing parameters.\n\nThe import and export prices are computed by evaluating user-supplied\nPython expression strings. Each expression may reference:\n\n- ``price``: the raw Nordpool spot price in EUR/kWh for that step.\n- ``ts``: the step start time as a UTC-aware ``datetime`` object.\n\nThis allows any supplier pricing scheme to be expressed without code\nchanges. For example, the Dutch Tibber all-in import price is::\n\n    ((price + 0.09161) * 1.21) + 0.0248\n\nBy default both formulas pass the raw spot price through unchanged.\n\nAttributes:\n    area: Nordpool area code (e.g. ``\"NO2\"``, ``\"NL\"``, ``\"SE3\"``).\n    import_formula: Python expression that computes the all-in import\n        price in EUR/kWh from the raw spot price. Applied to every step.\n    export_formula: Python expression that computes the net export price\n        in EUR/kWh from the raw spot price. Applied to every step.\n    price_interval: Length of one published price step \u2014\n        ``\"quarter_hourly\"`` or ``\"hourly\"``. Nordpool quotes day-ahead\n        prices on a 15-minute market time unit for most areas, which is\n        what ``\"quarter_hourly\"`` (the default) publishes unchanged. Set it\n        to ``\"hourly\"`` when your supplier bills a single price per whole\n        hour: the four quarter-hour spot prices are then averaged into one\n        hourly spot price before the formulas are applied, which is exactly\n        how such suppliers derive their hourly rate. Leaving it at\n        ``\"quarter_hourly\"`` on an hourly contract makes the optimiser\n        chase intra-hour price swings the meter never bills for.",
      "properties": {
        "area": {
          "description": "Nordpool price area code (e.g. 'NO2', 'NL', 'SE3').",
          "title": "Nordpool area",
          "type": "string"
        },
        "import_formula": {
          "default": "price",
          "description": "Python expression for the all-in import price in EUR/kWh. Available variables: ``price`` (raw spot, EUR/kWh), ``ts`` (datetime, UTC).",
          "title": "Import price formula",
          "type": "string"
        },
        "export_formula": {
          "default": "price",
          "description": "Python expression for the net export price in EUR/kWh. Available variables: ``price`` (raw spot, EUR/kWh), ``ts`` (datetime, UTC).",
          "title": "Export price formula",
          "type": "string"
        },
        "price_interval": {
          "default": "quarter_hourly",
          "description": "Length of one published price step. 'quarter_hourly' (default) publishes the raw Nordpool market time unit unchanged; 'hourly' averages each clock hour into a single step, matching suppliers that bill one dynamic price per hour.",
          "enum": [
            "hourly",
            "quarter_hourly"
          ],
          "title": "Price Interval",
          "type": "string",
          "ui_group": "basic",
          "ui_label": "Price interval"
        }
      },
      "required": [
        "area"
      ],
      "title": "NordpoolApiConfig",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Root configuration for the nordpool price fetcher daemon.\n\nArgs:\n    mqtt: MQTT broker connection settings.\n    mimir_topic_prefix: The ``mqtt.topic_prefix`` configured in mimirheim. Used\n        to derive default values for ``output_topic`` and\n        ``mimir_trigger_topic``. Defaults to ``\"mimir\"`` to match the mimirheim\n        default. Override when your mimirheim instance uses a different prefix.\n    trigger_topic: The tool subscribes here; a message fires one fetch cycle.\n    output_topic: Price payload is published retained to this topic. Defaults\n        to the mimirheim canonical price topic derived from ``mimir_topic_prefix``.\n    nordpool: Nordpool API and pricing formula parameters.\n    signal_mimir: If True, publish to mimir_trigger_topic after publishing prices.\n    mimir_trigger_topic: Required when signal_mimir is True. Defaults to the\n        mimirheim canonical trigger topic derived from ``mimir_topic_prefix``.",
  "properties": {
    "mqtt": {
      "$ref": "#/$defs/MqttConfig",
      "description": "MQTT broker connection settings.",
      "title": "MQTT"
    },
    "mimir_topic_prefix": {
      "default": "mimir",
      "description": "mimirheim mqtt.topic_prefix. Used to derive default output and trigger topics.",
      "title": "mimirheim topic prefix",
      "type": "string",
      "x-category": "Advanced"
    },
    "trigger_topic": {
      "description": "MQTT topic that triggers a fetch cycle.",
      "title": "Trigger topic",
      "type": "string",
      "x-category": "Advanced"
    },
    "output_topic": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "MQTT topic for the retained price payload. Defaults to '{mimir_topic_prefix}/input/prices' when not set.",
      "title": "Output topic",
      "x-category": "Advanced"
    },
    "nordpool": {
      "$ref": "#/$defs/NordpoolApiConfig",
      "description": "Nordpool API and pricing formula parameters.",
      "title": "Nordpool API"
    },
    "ha_discovery": {
      "anyOf": [
        {
          "$ref": "#/$defs/HomeAssistantConfig",
          "default": {}
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional Home Assistant MQTT discovery settings.",
      "title": "HA discovery",
      "x-category": "Advanced"
    },
    "stats_topic": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "MQTT topic where per-cycle run statistics are published.",
      "title": "Stats topic",
      "x-category": "Advanced"
    },
    "signal_mimir": {
      "default": false,
      "description": "Publish to mimir_trigger_topic after publishing prices.",
      "title": "Signal mimirheim",
      "type": "boolean",
      "x-category": "Advanced"
    },
    "mimir_trigger_topic": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Topic to trigger mimirheim. Derived from mimir_topic_prefix when not set.",
      "title": "mimirheim trigger topic",
      "x-category": "Advanced"
    }
  },
  "required": [
    "mqtt",
    "trigger_topic",
    "nordpool"
  ],
  "title": "NordpoolConfig",
  "type": "object",
  "x-categoryOrder": [
    "Basic",
    "Advanced"
  ],
  "x-format": "categories-vertical",
  "x-mimirheim": {
    "file": "nordpool.yaml",
    "category": "prices",
    "order": 0,
    "python_package": "nordpool",
    "python_model": "nordpool.config:NordpoolConfig",
    "exclusive_group": null,
    "required": false,
    "docs_url": null
  }
}
